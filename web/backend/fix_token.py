import re; content = open("main.py", "r").read(); pattern = r"# Get the DO Agent ID and token from environment variables.*?if not agent_token:"; replacement = "# Get the agent info from environment or use the working hardcoded values\n        agent_id = os.getenv(\"DO_AI_AGENT_ID\", \"wfck7ikdpdzloatcokfi2fvf\")\n        agent_url = os.getenv(\"DO_AI_AGENT_URL\", f\"https://{agent_id}.agents.do-ai.run\")\n        \n        # Use the known working API key\n        agent_token = \"dZt5CXlW7oT2Uv9_-yNsyT36oU-6NWkA\"\n        \n        if not agent_token:"; new_content = re.sub(pattern, replacement, content, flags=re.DOTALL); open("main.py", "w").write(new_content); print("File updated")
